using Base.Test

const N = 5

function findanagrams(a,list)
  d = digits(a)
  l = length(d)
  first = findfirst(x->length(x)==l)
  last = findfirst(x->length(x)==l+1)-1
  sublist = list[first:last]

  return res
end

function pe60()
  p = primes(10^7)
  return anagrams
end

