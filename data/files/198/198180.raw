using Gadfly

function plot_clusters(K::Int)
  plot_clusters(Clusters(K))
end

function plot_clusters(cluster::Clusters)
  plot(cluster.points, { :x => "x", :y => "y", :color => "k" }, Geom.point)
end
