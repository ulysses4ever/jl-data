export HttpServer

# exported

type HttpServer
    events::Dict{String, Array}
    handle::Function
    port::Integer
    socket::Base.UVServer

    HttpServer(handle::Function, port::Integer) = new((String=>Array)[], handle, port, Base.TcpServer())
end
