@doc """
Calculate a causal graph that is consistent with the data:

* `G` : graph of potential connections
* `df` : data frame of observed values for nodes in `G`
""" ->
function pc_algo(G::GenericGraph, df::DataFrame)
end
