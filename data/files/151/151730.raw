using LoraMPI
using MPI

function main()
  MPI.Init()

  mpistate = MPIState(MPI, 0)
  mcmodel = MCLikelihoodPatch()
  mcsampler = SMMALA()
  mcrunner = FrielMC(100:1:1000)

  MPI.Finalize()
end

main()
