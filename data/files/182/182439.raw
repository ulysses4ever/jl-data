# commutation(n, m)
# returns Magnus and Neudecker's commutation matrix of dimensions n by m
# Luca Brugnolini
# Based on Thomas P Minka (tpminka@media.mit.edu)

function commutation(n, m)

    k = reshape(kron(vec(eye(n)), eye(m)), n*m, n*m);

    return k
end
