#The learner-propagator component

function propagation_loop(word, initial_position)

table = create_blank_table()

#For each letter now known
    table = add_to_table(cipher[position], translation)

    iteration_limit = 5
    iteration_count = 0
    while (iteration_count != iteration_limit)
        words_found = 0

        #For position in cipher, scan for a partial match.
            #On partial match, learn if allowed by the current learning policy.

       if (words_found == 0)
           break
       end
       iteration_count = iteration_count + 1
    end
end
    



function march_word(word)

#For each valid position in the cipher
    result = propagation_loop(word, position)
    accumulate_result(result_to_date, result)
    

end


function main_loop()

#For each word in dictionary
    march_word(word)
    

end