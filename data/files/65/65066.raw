A = example2()
parent, post = etree(A, true)
A = A[post,post]
colptr, rowval = struct_L(A)
for i in 1:A.n
    println(rowval[colptr[i]:colptr[i+1]-1])
end

# test etree
A = example5()
L = example5(true)
@test Multifrontal._etree(L) == etree(A)
