using DataStructures

export cholesky!

function cholesky!(A::SparseMatrixCSC)
    m, n = size(A)
    m == n || throw(DimensionMismatch(""))
    FrontalStack = Stack(UpdateMatrix)
    for j = 1:n        
        locs = A.colptr[j]:A.colptr[j+1]-1
        # initialize frontal matrix
        F = FrontalMatrix()
        # elimination tree
        parent = etree(A)
        children = findin(parent, [j])
        for child in children
            # form the update matrix
            
            # form the frontal matrix
            
        end
        
    end
end
