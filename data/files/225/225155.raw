# Project Euler 1

# Attempt 1

# function Run()
    # tot = 0
	# i = 1
	# while i < 1000
		# if mod(i, 3) == 0 || mod(i, 5) == 0
			# tot += i
		# end
		# i += 1
	# end
	# tot
# end

# Final Version

function Run()
	tot = sum([x%3==0 || x%5==0 ? x : 0 for x=1:999])
end