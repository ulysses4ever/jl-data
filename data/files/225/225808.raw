#=
 * @description floating point equality comparison
 *  Ref: http:#floating-point-gui.de/errors/comparison/
 * @param a{Real}
 * @param b{Real}
 * @param [eps]{Float}
 * @returns {Bool}
 =#
function floatequal(a, b, eps=1e-12) 
  #eps default to 12 decimal places
  _a = abs(a)
  _b = abs(b)
  _d = abs(a - b)
  #shortcut, handles infinities
  if (a == b)
    return true
  #a or b or both are zero
  elseif (a * b == 0.0)
    return _d < (eps * eps)
  #use relative error
  else
    return (_d / (_a + _b)) < eps
  end
end