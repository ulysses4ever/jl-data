# simple proof that the view thing has some kind of issue.
_test_image = reshape(X[1,:], 20, 20)
_img = grayim(_test_image)
view(_img)
# =====================================================

# a short experiment on how to get the same results as in
# octave for a and a_b.
# Octave: f = a_b(a, :)
# looks like octave does a automatic matrix -> vector conversion...
# or more likly octave only knows vectors as 1xY or Yx1 matrecies.
a = [1 2 2 4 5 1]
a_b = eye(6)

f = a_b[a[:], :]
a
a[:]

