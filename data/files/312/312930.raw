#! /usr/bin/julia

# Rosetta Code, Check input device is a terminal

if isa(STDIN, Base.TTY)
    println("This program sees STDIN as a TTY.")
else
    println("This program does not see STDIN as a TTY.")
end
