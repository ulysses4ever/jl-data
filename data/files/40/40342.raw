using Distributions
using PyPlot
#normally we get brownian motion by integration of white noise
#we are using strides of different lengths. size 1, 2, etc

function midpoint_bm(maxlevel, sigma=1)
  gauss = Gaussian()
  X = [0 for i in 1:2049]
  st_devs = [sigma * power(0.5, (i+1)/2) for i in 1:maxlevel]
  for stride in something:
    for thing in strides:
      nothing
    end
  end
end
